extends ../layoutemp

block content
    .container(style="margin-top:2%;")
        h1 병실 현황
        .alert.alert-primary
            nav.nav
                a.nav-link.active(href='/patientmanagement/new') 환자 추가
                |   
                a.nav-link(href='/monitor') 환자 모니터링
                |   
                a.nav-link(href='/') 통계
                |
                a.nav-link(href='/patientmanagement/bed') 병실 현황
                |                 
                a.nav-link(href='/patientmanagement/outpatient') 퇴원 환자
                |    
    .container(style="margin-top:2%;")

        .alert.alert-secondary
            include ./_bed
        
        .alert.alert-primary
            .row
                .col-md-2
                    .btn.btn-primary.usedbed
                        | 사용중인 침대 정보
            
        table.table.usedbedtable
            thead
                tr
                    th(scope="col") 과
                    th(scope="col") 침대번호
                    th(scope="col") 환자번호(둘까?)
                    th(scope="col") 환자이름 
                    th(scope="col") 출생년도 
                    th(scope="col") 담당의사 
                    th(scope="col") 질병
                    th(scope="col") 입원일
            tbody
                each patient in patientList
                    tr
                        th
                        td(scope="row") #{patient.bed_no}
                        td
                            a(href=`/patientmanagement/show/${patient.patient_id}`) #{patient.patient_id}
                        td    
                        td    
                        td    
                        td #{patient.disease_name}
                        td #{moment(patient.hospital_day).format('YYYY-MM-DD')}
    
